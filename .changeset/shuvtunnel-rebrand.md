---
"shuvtunnel": minor
---

Rebrand the CLI as ShuvTunnel. The `shuvtunnel` package and binary default to the shuv.zip API and store state under `shuvtunnel` XDG directories. The bridge now negotiates the `shuvtunnel` WebSocket subprotocol.
